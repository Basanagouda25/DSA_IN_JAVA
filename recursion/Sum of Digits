class Solution {
    public static int sumOfDigits(int n) {
        // Code here
        if(n == 0){
            return 0;
        }
        int rem = n % 10;
        return sumOfDigits(n/10) + rem;
    }
}
